import Foundation

/// アプリ内の「ご意見・ご要望 / 不具合報告」導線。
///
/// バックエンドを持たないローカルファースト構成のため、個人の連絡先 (メール) を
/// アプリ本体に埋め込まず、Google フォームへ集約する。フォーム側に「アプリの
/// バージョン」「端末・iOS バージョン」の任意項目を用意しているので、利用者は
/// そこへ記入できる (旧 mailto 版の診断情報自動付与の代替)。
///
/// この URL はプライバシーポリシー / 利用規約 / サポートページの「お問い合わせ」と
/// 同一の窓口。
enum FeedbackComposer {
    /// お問い合わせフォーム (公開窓口)。
    static let supportFormURL = URL(string: "https://forms.gle/Ljbaj4MvW2YPmyJ99")!
}
